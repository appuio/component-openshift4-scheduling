parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-scheduling/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - openshift4-scheduling/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-scheduling/
